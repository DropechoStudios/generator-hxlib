language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-haxepunk' ]; then cd .. && eval "mv $currentfolder generator-haxepunk" && cd generator-haxepunk; fi

